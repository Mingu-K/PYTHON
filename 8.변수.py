# 변수란 객체를 가리키는 말이고 변수는 변수의 값이 저장된 메모리 주소를 가리키는 것이다.


# 주소확인하기 id(a)
# a = [1,2,3]
# print(id(a))            # id(a)를 통해서 변수의 값이 저장된 주소를 확인할 수 있다.

# 변수 a의 값을 변수 b가 복사를 해도 결국 a의 주소 값을 복사한 것이 되어 id()를 입력시 같은 값이 출력된다.